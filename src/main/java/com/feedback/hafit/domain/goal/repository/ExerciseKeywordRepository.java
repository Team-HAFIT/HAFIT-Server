package com.feedback.hafit.domain.goal.repository;

import com.feedback.hafit.domain.goal.entity.ExerciseKeyword;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExerciseKeywordRepository extends JpaRepository<ExerciseKeyword, Long> {
}
