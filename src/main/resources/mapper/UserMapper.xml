<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feedback.hafit.mapper.UserMapper">
    <!-- 회원가입 -->
    <insert id="userJoin" parameterType="UerDTO">
        INSERT INTO user(email, password, name, phone)
        VALUES(#{email}, #{password}, #{name}, #{phone})
    </insert>

    <!-- 아이디 중복 검사 -->
    <select id="emailCheck" resultType="int">
        SELECT COUNT(*) from user
        WHERE email = #{email}
    </select>

    <!-- 로그인 -->
    <select id="login" resultType="com.feedback.hafit.entity.UserDTO">
        SELECT email, password
        FROM user
        WHERE email = #{email}
        AND password = #{password}
    </select>
</mapper>
